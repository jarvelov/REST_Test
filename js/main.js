var REST = angular.module("REST", ['$scope']);

REST.controller(
    "optionController",
    function($scope) {
        //Code here
    }
);
REST.controller(
    "addUserController",
    function($scope) {
        //Code here

        $scope.addUserForm = function() {
            return 'addUserForm';
        }

        $scope.addUserForm = function() {
            return 'addUserForm';
        }
    }
);
