angular.module("REST", [])
    .controller(
        "optionController",
        function($scope) {
            //Code here
        }
    );
    .controller(
        "addUserController",
        function($scope) {
            //Code here

            $scope.addUserForm = function() {
                return 'addUserForm';
            }

            $scope.addUserForm = function() {
                return 'addUserForm';
            }
        }
    );
