angular.module("REST", [])
    .controller(
        "optionController",
        function($scope) {
            //Code here
        },
        "addUserController",
        function($scope) {
            //Code here

            $scope.addUserForm = function() {
                return 'addUserForm';
            }

            $scope.addUserForm = function() {
                return 'addUserForm';
            }
        }
    );